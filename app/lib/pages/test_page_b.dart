import 'package:flutter/material.dart';

class TestPageB extends StatelessWidget {
  const TestPageB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Test Page B'),
      ),
    );
  }
}
