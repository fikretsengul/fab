import 'package:flutter/material.dart';

@immutable
final class Palette {
  const Palette._();

  // Colors
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);

  static const jetstream = Color(0xFFBECFBB);
  static const lenurple = Color(0xFFC28ED1);
  static const crayola = Color(0xFFBCCBEA);
  static const bone = Color(0xFFE7DCC8);

  // Scheme
  static const background = Color(0xFFEDF0EB);
  static const foreground = Color(0xFF000000);
}
